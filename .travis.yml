language: generic

before_install:
  - sudo apt-get update
  - sudo apt-get install python3
  - python -m pip install --upgrade pip
install:
  - pip install --user -r requirements.txt
script:
  - python -m pytest

branches:
  only:
    - master
